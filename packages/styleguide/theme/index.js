import colors from '../colors'

const theme = {
    palette: {
        primary: {
            light: colors.blue.light[0],
            main: colors.blue.main,
            dark: colors.blue.dark[0],
            contrastText: "white"
        },
        secundary: {
            light: colors.red.light[0],
            main: colors.red.main,
            dark: colors.red.dark[0],
            contrastText: "white"
        },
    }
}

export default theme